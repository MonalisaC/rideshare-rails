<!-- todo: need to move conditional out of view -->
<h1 class="page-header">Passengers List</h1>
<ul>
  <% @passengers.each do |passenger| %>
  <% if passenger.is_deactivated != true %>
  <li>
    <span class="pass-name">
      <%= link_to passenger.name, passenger_path(passenger.id),
      class: 'pass-name-link'  %>.
    </span>
    Phone:  <%="#{passenger.phone_num}"%>
  </li>
  <% end%>
  <% end%>
</ul>
